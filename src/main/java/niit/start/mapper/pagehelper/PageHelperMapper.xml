<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="niit.start.mapper.pagehelper.PageHelperMapper">

    <select id="selectByPageNumSize" resultType="User">
        select * from user
    </select>

    <select id="selectByPageHelperUser" parameterType="PageHelperUser" resultType="PageHelperUser">
        select * from user
    </select>

</mapper>